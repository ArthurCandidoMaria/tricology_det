# Porta do backend FastAPI no ambiente local.
# 8000 costuma estar ocupada por outros containers Docker.
BACKEND_PORT=8008

# Deixe VITE_API_URL vazio: o Vite (dev) e o nginx (prod) fazem o proxy de /api.
VITE_API_URL=
